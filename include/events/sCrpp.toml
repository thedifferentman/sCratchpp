version = 1

[package]
name = "events"
version = "0.1.0"
resource_namespace = "__scl_events"

[library]
sources = ["events.cpp"]
include_dirs = ["include"]

[[events]]
type = "wheel"
queue = "wheel"
enabled = "__scl_events_enabled"
capacity = 128

[[events]]
type = "keyboard"
queue = "keyboard"
enabled = "__scl_events_enabled"
capacity = 128
